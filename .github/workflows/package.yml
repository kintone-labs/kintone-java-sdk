name: Package
on: push

jobs:
  packaging:
    name: Packaging
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-java@v1
        with:
          java-version: '9.0.4'
      - run: mvn package
